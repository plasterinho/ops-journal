# Things the platform team owns

Cluster-scoped, slow-moving, opinionated.

## Examples

* GitOps controller manifests
* ingress controller
* CRDs
* shared namespaces
* baseline policies

## Rule

If breaking this can break *everything*, it belongs here.
